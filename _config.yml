title: yamamoto-ryuzo portal
theme: jekyll-theme-minimal

plugins:
  - jekyll-redirect-from

defaults:
  - scope:
      path: ""
    values:
      layout: default

# フロントマターなしのMDファイルも変換する
include:
  - "**/*.md"
