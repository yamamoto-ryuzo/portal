title: yamamoto-ryuzo portal
theme: jekyll-theme-minimal
toc_depth: 4

# Note: `jekyll-redirect-from` removed because GitHub Pages' safe build
# environment does not support some plugins. Re-enable if using a custom
# build (Actions) that supports this plugin.
plugins: []

defaults:
  - scope:
      path: ""
    values:
      layout: default

# フロントマターなしのMDファイルも変換する
include:
  - "**/*.md"
